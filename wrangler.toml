# Qalam - Cloudflare Pages (Static Export)
# This is a static Next.js export. Data is served from the Worker/R2.
# For the Worker configuration, see ./worker/wrangler.toml
name = "qalam"
compatibility_date = "2024-12-24"
pages_build_output_dir = "out"
